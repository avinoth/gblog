
  <%= link_to 'New Article', new_article_path %>

<br>
    <% @articles.each do |article| %>
      <h2>
      <%= link_to article.title, article %>
      </h2>
        <p><%= article.content %></p>
      Tags: <span class="label label-info"> <%= raw article.tag_list.map { |t| link_to t, tag_path(t) }.join(' - ') %></span>
      
      <br>
        
        
    <% end %>
  